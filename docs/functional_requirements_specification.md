# PESC MATE 기능요구사양서

| 항목 | 내용 |
|---|---|
| 문서명 | PESC MATE 기능요구사양서 (FRS) |
| 문서 버전 | v1.0 |
| 작성일 | 2026-09-07 |
| 대상 시스템 | AI 기반 온라인 PECS 의사소통 플랫폼 PESC MATE |
| 기준 문서 | `docs/project_plan.md`, `README.md` |
| 문서 상태 | 개발 기준안 |

## 기능요구사양 정의 (과제 제출용)

본 절은 과제 계획서의 주요 제공 기능을 기능 설명, 동작 환경, 입력 및 출력 순서로 정의한다. 아래 내용은 개발 목표이며 현재 구현 완료 상태를 나타내지 않는다.

### 1. 그림 카드 탐색 및 선택 기능

- 기능: 사용자는 카테고리별 그림 카드를 조회하고 원하는 카드를 선택한다. 선택한 카드는 문장 구성 영역에 순서대로 표시되며, 개별 삭제, 순서 변경 및 전체 초기화가 가능하다.
- 동작 환경: 웹 브라우저가 설치된 PC, 태블릿 또는 스마트폰과 웹 서버 연결이 필요하다. 터치스크린 또는 마우스·키보드로 조작한다. 카드 이미지와 카테고리 데이터가 서버에 준비되어야 한다.
- 입력: 카테고리 선택, 그림 카드 선택, 카드 이동·삭제 및 초기화 명령을 입력한다.
- 출력: 카드 선택 화면에 카테고리 메뉴, 그림·이름을 포함한 카드 목록, 선택 카드 영역, 삭제·순서 변경·초기화 버튼 및 문장 생성 버튼이 표시된다.

### 2. 사용자별 이용 기록 저장 기능

- 기능: 시스템은 사용자별 카드 선택 내용, 선택 순서, 사용 시각 및 카드 조합을 저장한다. 저장된 기록은 개인 맞춤 추천과 이용 현황 분석에 활용된다.
- 동작 환경: 사용자 구분을 위한 계정 또는 프로필, 백엔드 서버 및 데이터베이스가 필요하다. 기록 저장 시 클라이언트와 서버, 서버와 데이터베이스 사이의 연결이 필요하다.
- 입력: 사용자 식별자, 선택 카드, 선택 순서, 사용 시각 및 의사소통 세션 정보가 입력된다.
- 출력: 데이터베이스에 사용자별 이용 기록이 저장된다. 저장 실패 시 화면에 실패 안내와 재시도 버튼이 표시된다.

### 3. 개인 맞춤형 카드 추천 기능

- 기능: 시스템은 사용자의 카드 사용 빈도, 최근 사용 기록 및 함께 선택한 카드 조합을 분석하여 다음에 사용할 가능성이 높은 카드를 추천한다. 기록이 없는 사용자에게는 기본 추천 카드가 제공된다.
- 동작 환경: 카드 데이터, 사용자별 이용 기록, 추천 알고리즘을 실행하는 서버가 필요하다. 초기에는 빈도·최근성·연관 규칙 기반으로 구현하며 외부 유료 AI 서비스 가입을 필수 조건으로 두지 않는다.
- 입력: 사용자 식별자, 과거 카드 이용 기록 및 현재 선택한 카드 목록이 입력된다.
- 출력: 카드 선택 화면의 추천 영역에 추천 카드의 그림과 이름이 표시된다. 추천 카드를 선택하면 문장 구성 영역에 추가된다.

### 4. 선택 카드 기반 문장 생성 기능

- 기능: 시스템은 선택한 카드의 의미와 순서를 분석하여 자연스러운 한국어 문장을 생성한다. 예를 들어 ‘나 → 물 → 마시다’를 선택하면 ‘저는 물을 마시고 싶어요.’와 같은 문장이 생성된다. 사용자는 생성 결과를 확인하고 재생성을 요청한다.
- 동작 환경: 문장 생성 모델 또는 자연어 처리 모듈을 실행할 환경이 필요하다. 외부 AI API를 사용하는 경우 인터넷 연결, 해당 서비스 계정, API 키 및 사용량에 따른 이용 요금이 필요할 수 있다. 공급자와 요금제는 선정 후 확정한다. 자체 모델을 사용하는 경우 모델 실행에 필요한 메모리와 연산 장비를 별도로 확보한다.
- 입력: 순서가 있는 선택 카드 목록, 카드의 의미 및 문장 생성·재생성 명령을 입력한다.
- 출력: 문장 확인 영역에 생성 문장, 처리 중 상태, 재생성 버튼 및 음성 출력 버튼이 표시된다. 생성 실패 시 오류 안내와 재시도 버튼이 표시된다.

### 5. 문장 음성 출력 기능

- 기능: 시스템은 사용자가 확인한 문장을 TTS로 변환하여 음성으로 출력한다. 사용자는 음성을 재생하거나 중지한다.
- 동작 환경: 한국어를 지원하는 TTS 엔진과 PC·태블릿·스마트폰의 스피커 또는 외부 스피커가 필요하다. 시연 전 음량과 한국어 음성 지원 여부를 확인한다. 외부 TTS API를 사용하는 경우 인터넷 연결과 API 인증 정보가 필요하며 비용 발생 여부는 선정한 서비스에 따라 확정한다.
- 입력: 확인된 문장 텍스트와 재생·중지 명령을 입력한다.
- 출력: 기기 스피커에서 해당 문장의 음성이 출력되며 화면에는 문장 텍스트, 재생 상태 및 재생·중지 버튼이 표시된다.

### 6. 보호자·교사용 이용 분석 기능

- 기능: 보호자 또는 교사는 연결된 사용자의 카드 사용 횟수, 자주 선택한 카드, 카테고리별 이용 빈도 및 자주 사용한 카드 조합을 확인한다. 선택한 사용자와 기간에 따라 분석 결과가 갱신된다.
- 동작 환경: 권한이 부여된 보호자·교사 계정, 사용자 연결 정보, 누적 이용 기록, 데이터베이스 및 분석 API가 필요하다. 웹 브라우저가 있는 PC, 태블릿 또는 스마트폰에서 조회한다.
- 입력: 분석 대상 사용자와 조회 시작일·종료일을 입력한다.
- 출력: 대시보드에 사용자 선택 메뉴, 기간 선택 항목, 총 사용 횟수, 자주 사용한 카드 목록, 카테고리별 그래프 및 카드 조합 표가 표시된다. 기록이 없으면 데이터 없음 안내가 표시된다.

### 공통 화면 및 메뉴 구성

| 화면 | UI 구성 및 메뉴 |
|---|---|
| 사용자 진입 화면 | 계정 로그인 또는 사용자 프로필 선택, 카드 사용 화면 진입 |
| 카드 사용 화면 | 카테고리 메뉴, 카드 목록, 추천 카드 영역, 선택 카드 영역, 선택 편집 버튼, 문장 생성 버튼 |
| 문장 확인 영역 | 카드 사용 화면 내 생성 문장, 처리 상태, 재생성 버튼, 음성 재생·중지 버튼 |
| 보호자·교사 대시보드 | 연결 사용자 선택, 조회 기간 선택, 사용 횟수 요약, 카드 순위, 카테고리 그래프, 카드 조합 표 |

일반 사용자는 카드 사용 화면으로 이동하고, 보호자·교사는 권한에 따라 연결된 사용자의 대시보드로 이동한다. 화면 출력은 PC 모니터 또는 스마트폰·태블릿 디스플레이를 사용하고, 조작은 터치 또는 마우스·키보드를 사용한다. 별도 Character Display, 센서, 부저 및 LED는 본 시스템의 필수 장치에 포함하지 않는다.

### 공통 운영 및 시연 환경

- 웹 클라이언트와 백엔드 서버를 실행할 환경 및 카드·이용 기록을 저장할 데이터베이스를 준비한다. 현재 개발 환경은 React/Vite, FastAPI 및 Docker Compose를 사용하며 데이터베이스 제품은 별도 선정한다.
- 원격 서버 또는 외부 AI·TTS API에 접속하는 구성에서는 인터넷 연결이 필요하다. 경진대회 현장에서 네트워크가 제공되지 않는 경우 휴대전화 테더링이나 모바일 핫스폿과 충분한 데이터 용량을 준비한다.
- 노트북에서 서버를 실행하고 스마트폰·태블릿으로 시연하는 경우 두 기기를 같은 네트워크에 연결한다. 현재 로컬 실행 설정의 서버 주소, 포트 공개 범위 및 접근 허용 설정은 다중 기기 시연에 맞게 조정한다.
- 현장 인터넷 없이 시연하려면 카드 데이터, 데이터베이스, 문장 생성 및 한국어 TTS를 모두 로컬에서 실행할 수 있도록 별도 구성해야 한다. 현재 저장소만으로 오프라인 동작이 보장되지는 않는다.
- 시연용 사용자 프로필, 카드 이미지, 예시 이용 기록 및 스피커를 준비하고 카드 선택부터 문장 생성·음성 출력·대시보드 조회까지 사전 점검한다.

## 1. 문서 목적

본 문서는 PESC MATE의 개발, 검수 및 인수 기준이 되는 기능 요구사항을 정의한다. PESC MATE는 PECS(Picture Exchange Communication System) 카드를 디지털화하고, 사용자별 카드 추천, 선택 카드 기반 문장 생성, 음성 출력 및 이용 데이터 분석을 하나의 웹 서비스로 제공한다.

본 문서에서 사용하는 요구사항 키워드는 다음과 같다.

- **필수(Must)**: 1차 출시 또는 과제 완료를 위해 반드시 구현해야 한다.
- **권장(Should)**: 사용자 경험과 운영 품질을 위해 구현을 권장한다.
- **향후(Could)**: 초기 범위에는 포함하지 않으며 후속 버전에서 검토한다.

## 2. 시스템 범위

### 2.1 포함 범위

- 사용자 계정 및 역할별 접근 관리
- PECS 카드와 카테고리의 조회 및 관리
- 카드 검색, 탐색, 선택, 순서 변경 및 초기화
- 개인별 카드 이용 이력 저장
- 이용 이력 기반 맞춤 카드 추천
- 선택 카드 기반 자연어 문장 생성 및 편집
- 생성 문장의 TTS 음성 출력
- 보호자·교사용 이용 분석 대시보드
- PC, 태블릿 및 스마트폰을 지원하는 반응형 웹 UI

### 2.2 제외 범위

- 의료적 진단이나 치료 효과 판정
- 응급 상황 자동 신고
- 오프라인 전용 네이티브 앱
- 외부 병원·학교 정보시스템과의 연동
- 초기 버전의 사용자가 직접 학습시키는 AI 모델

### 2.3 현재 구현 기준

현재 저장소에는 React/Vite 기반 웹 클라이언트, FastAPI 기반 API 서버, Docker Compose 실행 환경 및 `/api/health` 상태 확인 기능이 구현되어 있다. 본 문서의 나머지 기능은 목표 요구사항이며 구현 완료를 의미하지 않는다.

## 3. 사용자 및 권한

| 역할 | 설명 | 주요 권한 |
|---|---|---|
| PECS 사용자 | 그림 카드로 의사를 표현하는 서비스의 주 사용자 | 카드 조회·선택, 추천 이용, 문장 생성·확인, 음성 출력 |
| 보호자/교사 | 사용자 이용을 지원하고 기록을 확인하는 담당자 | 연결된 사용자의 카드 설정, 이용 이력 및 분석 조회 |
| 관리자 | 서비스와 공통 콘텐츠를 운영하는 담당자 | 사용자·권한, 공통 카드·카테고리 및 시스템 상태 관리 |

권한은 최소 권한 원칙을 적용한다. 보호자/교사는 명시적으로 연결된 사용자의 정보만 조회할 수 있어야 한다.

## 4. 주요 이용 흐름

1. 사용자가 로그인하거나 지정된 사용자 프로필을 선택한다.
2. 카테고리, 검색 또는 추천 영역에서 카드를 찾는다.
3. 카드를 선택하면 선택 영역에 선택 순서대로 추가된다.
4. 사용자는 카드의 순서를 변경하거나 일부/전체 선택을 취소한다.
5. 사용자가 문장 생성을 요청하면 시스템이 카드 의미와 순서를 반영한 문장을 제시한다.
6. 사용자가 문장을 확인하거나 허용된 범위에서 수정한다.
7. 사용자가 음성 출력 버튼을 누르면 시스템이 문장을 음성으로 재생한다.
8. 시스템은 카드 선택, 문장 생성 및 음성 출력 이력을 저장한다.
9. 보호자/교사는 대시보드에서 연결된 사용자의 이용 패턴을 확인한다.

## 5. 기능 요구사항

### 5.1 계정, 인증 및 사용자 관리

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-AUTH-001 | 시스템은 사용자 계정으로 로그인 및 로그아웃할 수 있어야 한다. | 필수 | 유효한 계정은 로그인 후 역할별 첫 화면으로 이동하고, 잘못된 인증 정보에는 구체적인 실패 메시지를 표시한다. 로그아웃 후 보호 화면에 다시 접근할 수 없다. |
| FR-AUTH-002 | 시스템은 PECS 사용자, 보호자/교사, 관리자의 역할을 구분해야 한다. | 필수 | 각 역할로 로그인했을 때 허용된 메뉴와 API만 접근 가능하며 권한 없는 요청은 거부된다. |
| FR-AUTH-003 | 보호자/교사는 자신에게 연결된 PECS 사용자 프로필을 선택할 수 있어야 한다. | 필수 | 연결 사용자 목록이 표시되고, 선택 변경 시 카드 설정·추천·분석 데이터의 대상이 함께 변경된다. |
| FR-AUTH-004 | 관리자는 계정의 활성 상태와 역할을 관리할 수 있어야 한다. | 권장 | 관리자가 계정을 비활성화하면 해당 계정의 신규 로그인이 차단되고 변경 이력이 기록된다. |
| FR-AUTH-005 | 시스템은 일정 시간 미사용 시 인증 세션을 만료해야 한다. | 권장 | 설정된 만료 시간 이후 보호 기능 요청 시 재로그인을 요구한다. |

### 5.2 PECS 카드 및 카테고리

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-CARD-001 | 시스템은 카드의 이미지, 표시명, 의미, 카테고리 및 활성 상태를 관리해야 한다. | 필수 | 필수 정보가 없는 카드는 저장되지 않으며 활성 카드만 일반 사용자에게 표시된다. |
| FR-CARD-002 | 사용자는 카테고리별로 카드를 탐색할 수 있어야 한다. | 필수 | 카테고리를 선택하면 해당 카테고리의 활성 카드만 표시되고 현재 선택 카테고리가 구분된다. |
| FR-CARD-003 | 사용자는 카드명 또는 의미의 검색어로 카드를 찾을 수 있어야 한다. | 필수 | 검색어 입력 후 일치 카드가 표시되며 결과가 없을 때 빈 결과 안내가 제공된다. |
| FR-CARD-004 | 시스템은 카드 목록을 페이지 또는 점진적 로딩 방식으로 제공해야 한다. | 권장 | 카드 수가 많아도 중복·누락 없이 추가 항목을 조회할 수 있다. |
| FR-CARD-005 | 보호자/교사 또는 관리자는 권한 범위 내에서 카드를 등록·수정·비활성화할 수 있어야 한다. | 권장 | 지원 형식과 용량을 벗어난 이미지는 거부하며, 변경 결과가 카드 화면에 반영된다. 사용 이력이 있는 카드는 물리 삭제 대신 비활성화할 수 있다. |
| FR-CARD-006 | 시스템은 카드 이미지가 로드되지 않을 때 대체 이미지와 카드명을 표시해야 한다. | 필수 | 이미지 오류가 발생해도 카드의 의미를 식별하고 선택할 수 있다. |

### 5.3 카드 선택과 문장 보드

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-BOARD-001 | 사용자가 카드를 선택하면 문장 보드에 선택 순서대로 추가해야 한다. | 필수 | 카드 선택 직후 보드에 표시되고 선택 순서가 유지된다. |
| FR-BOARD-002 | 사용자는 보드에서 개별 카드를 제거할 수 있어야 한다. | 필수 | 제거한 카드만 사라지고 나머지 카드의 상대적 순서는 유지된다. |
| FR-BOARD-003 | 사용자는 선택 카드의 순서를 변경할 수 있어야 한다. | 필수 | 이동 후 화면 순서와 서버에 전달되는 카드 순서가 일치한다. |
| FR-BOARD-004 | 사용자는 확인 절차를 거쳐 문장 보드를 전체 초기화할 수 있어야 한다. | 필수 | 취소 시 보드가 유지되고 확인 시 모든 선택 카드가 제거된다. |
| FR-BOARD-005 | 시스템은 중복 카드 선택을 허용해야 한다. | 필수 | 같은 카드를 여러 번 선택하면 각각 독립 항목으로 보드에 표시된다. |
| FR-BOARD-006 | 시스템은 설정된 최대 선택 개수를 초과하는 선택을 방지해야 한다. | 권장 | 한도 도달 시 추가 선택이 차단되고 이유와 해결 방법을 안내한다. |
| FR-BOARD-007 | 화면을 실수로 새로고침한 경우 작성 중인 보드를 복구할 수 있어야 한다. | 권장 | 동일 기기·동일 사용자 세션에서 미완료 선택 내용이 복원된다. |

### 5.4 개인 맞춤 카드 추천

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-REC-001 | 시스템은 사용자의 카드 사용 빈도, 최근 사용 및 카드 조합을 바탕으로 추천 카드를 제공해야 한다. | 필수 | 추천 API 응답에 카드와 추천 점수 또는 순위가 포함되며 다른 사용자 데이터가 섞이지 않는다. |
| FR-REC-002 | 현재 문장 보드의 선택 내용을 다음 카드 추천에 반영해야 한다. | 필수 | 보드의 마지막 선택이 바뀌면 추천 결과를 다시 요청하고 갱신된 결과를 표시한다. |
| FR-REC-003 | 이용 이력이 부족한 신규 사용자에게 기본 추천을 제공해야 한다. | 필수 | 이력이 없어도 관리자가 지정한 기본 카드 또는 전체 인기 카드가 표시된다. |
| FR-REC-004 | 사용자는 추천 카드도 일반 카드와 동일하게 선택할 수 있어야 한다. | 필수 | 추천 카드 선택 시 문장 보드에 정상 추가되고 이용 이력에 추천 경유 여부가 기록된다. |
| FR-REC-005 | 추천 기능이 실패해도 일반 카드 탐색 및 선택은 계속 사용할 수 있어야 한다. | 필수 | 추천 실패 안내를 표시하되 카드 목록과 문장 보드는 정상 동작한다. |
| FR-REC-006 | 추천 모델 또는 알고리즘의 버전을 결과와 이력에 기록해야 한다. | 권장 | 각 추천 노출/선택 기록에서 적용된 추천 버전을 식별할 수 있다. |

### 5.5 자연어 문장 생성

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-SENT-001 | 시스템은 선택 카드의 의미와 순서를 입력으로 자연스러운 한국어 문장을 생성해야 한다. | 필수 | 카드가 한 장 이상일 때 생성할 수 있고, 응답에 생성 문장과 원본 카드 순서가 포함된다. |
| FR-SENT-002 | 생성 문장은 선택 카드의 핵심 의미를 임의로 누락하거나 반대로 변경해서는 안 된다. | 필수 | 사전 정의된 검수 시나리오에서 카드의 핵심 대상·행동·부정 의미가 생성 문장에 보존된다. |
| FR-SENT-003 | 카드가 선택되지 않은 상태에서는 문장 생성을 요청할 수 없어야 한다. | 필수 | 생성 버튼이 비활성화되거나 요청 시 카드 선택 안내가 표시된다. |
| FR-SENT-004 | 사용자는 생성된 문장을 확인하고 다시 생성할 수 있어야 한다. | 필수 | 재생성 시 새 결과가 표시되며 이전 결과와 생성 시점은 이력으로 구분된다. |
| FR-SENT-005 | 권한 또는 설정에 따라 사용자는 생성 문장을 음성 출력 전에 수정할 수 있어야 한다. | 권장 | 허용된 길이 안에서 편집한 문장이 저장되고 TTS 입력에도 동일하게 반영된다. |
| FR-SENT-006 | AI 문장 생성이 실패하거나 제한 시간을 초과하면 규칙 기반 대체 문장 또는 재시도 수단을 제공해야 한다. | 필수 | 실패 원인을 이해할 수 있는 안내와 재시도 버튼이 표시되며 선택 카드는 보존된다. |
| FR-SENT-007 | 유해하거나 개인정보를 과도하게 추론하는 생성 결과를 제한해야 한다. | 필수 | 안전 정책에 해당하는 출력은 사용자에게 노출하지 않고 안전한 대체 표현 또는 안내를 제공한다. |

### 5.6 TTS 음성 출력

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-TTS-001 | 시스템은 확인된 문장을 한국어 음성으로 재생할 수 있어야 한다. | 필수 | 음성 출력 요청 후 생성 문장과 동일한 내용의 음성이 재생된다. |
| FR-TTS-002 | 사용자는 음성을 재생, 일시 정지 또는 중지할 수 있어야 한다. | 필수 | 각 조작에 따라 재생 상태와 버튼 상태가 즉시 일치한다. |
| FR-TTS-003 | 중복 재생 요청 시 여러 음성이 겹쳐 재생되지 않아야 한다. | 필수 | 새 재생을 시작하면 기존 재생을 중지하거나 동일 요청을 무시한다. |
| FR-TTS-004 | TTS 실패 시 문장 텍스트를 유지하고 재시도 방법을 제공해야 한다. | 필수 | 실패 안내 후에도 문장 확인·편집·복사가 가능하다. |
| FR-TTS-005 | 사용자는 제공되는 범위에서 음성 종류와 속도를 설정할 수 있어야 한다. | 향후 | 선택 설정이 다음 재생에 적용되고 사용자 설정으로 저장된다. |

### 5.7 이용 이력 및 데이터 수집

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-HIST-001 | 시스템은 사용자, 카드, 선택 순서 및 선택 시각을 이용 이력으로 저장해야 한다. | 필수 | 카드 선택 완료 후 해당 사용자의 이력에서 카드 ID, 순서, 시각을 조회할 수 있다. |
| FR-HIST-002 | 시스템은 생성 문장, 생성 시각, 사용 카드 및 생성 성공 여부를 저장해야 한다. | 필수 | 문장 기록과 원본 카드 선택 기록을 연결하여 조회할 수 있다. |
| FR-HIST-003 | 시스템은 TTS 요청 시각과 성공 여부를 저장해야 한다. | 필수 | 통계에서 음성 출력 횟수와 실패 건수를 구분할 수 있다. |
| FR-HIST-004 | 사용자의 명시적 완료 동작을 하나의 의사소통 세션으로 구분해야 한다. | 필수 | 한 세션 안에서 선택 카드, 생성 문장 및 음성 출력 기록이 연결된다. |
| FR-HIST-005 | 권한 있는 사용자는 기간을 지정하여 이용 이력을 조회할 수 있어야 한다. | 권장 | 시작일과 종료일 조건에 맞는 결과만 반환되며 최신순 정렬을 지원한다. |
| FR-HIST-006 | 개인정보 삭제 요청 또는 보존 기간 만료 시 정책에 따라 데이터를 삭제 또는 비식별화할 수 있어야 한다. | 필수 | 처리 후 개인 식별 정보로 해당 기록을 조회할 수 없고 처리 결과가 감사 기록에 남는다. |

### 5.8 보호자·교사 대시보드

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-DASH-001 | 보호자/교사는 연결된 사용자와 조회 기간을 선택할 수 있어야 한다. | 필수 | 사용자 또는 기간 변경 시 모든 지표가 같은 조건으로 갱신된다. |
| FR-DASH-002 | 대시보드는 총 카드 선택 횟수, 자주 사용한 카드 및 카테고리별 빈도를 제공해야 한다. | 필수 | 원시 이력으로 재계산한 값과 화면 지표가 일치한다. |
| FR-DASH-003 | 대시보드는 자주 함께 사용한 카드 조합을 제공해야 한다. | 필수 | 설정된 최소 횟수 이상의 조합을 빈도순으로 확인할 수 있다. |
| FR-DASH-004 | 대시보드는 기간별 이용 추이를 그래프 또는 표로 제공해야 한다. | 필수 | 일·주·월 중 지원 단위로 집계되고 데이터가 없는 구간도 명확히 표현된다. |
| FR-DASH-005 | 대시보드는 데이터가 없을 때 오류 화면 대신 빈 상태 안내를 제공해야 한다. | 필수 | 대상 기간에 기록이 없으면 0 또는 빈 상태와 안내 문구를 표시한다. |
| FR-DASH-006 | 보호자/교사는 분석 결과를 CSV 등 파일로 내려받을 수 있어야 한다. | 향후 | 선택 사용자와 기간의 데이터만 내려받으며 파일에 생성 시점과 집계 기준이 포함된다. |

### 5.9 관리자 및 운영

| ID | 요구사항 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-ADM-001 | 관리자는 공통 카테고리의 이름, 순서 및 활성 상태를 관리할 수 있어야 한다. | 권장 | 변경 내용이 사용자 카드 탐색 화면에 설정 순서대로 반영된다. |
| FR-ADM-002 | 관리자는 공통 카드와 카테고리의 중복 및 참조 관계를 확인할 수 있어야 한다. | 권장 | 사용 중인 카테고리 비활성화 시 영향 범위를 안내하고 확인을 요구한다. |
| FR-ADM-003 | 시스템은 서비스 상태를 확인하는 헬스 체크 API를 제공해야 한다. | 필수 | `GET /api/health`가 정상 시 HTTP 200과 `status: ok`를 반환한다. |
| FR-ADM-004 | 관리 작업과 개인정보 관련 조회·변경은 감사 로그로 기록해야 한다. | 필수 | 행위자, 작업, 대상, 결과 및 시각을 조회할 수 있으며 일반 사용자는 감사 로그에 접근할 수 없다. |

## 6. 외부 및 내부 인터페이스 요구사항

### 6.1 사용자 인터페이스

- 주요 조작은 이미지와 쉬운 한국어 레이블을 함께 제공한다.
- 카드 선택, 제거, 재생 등 사용 결과를 시각적으로 즉시 피드백한다.
- 핵심 의사소통 흐름은 스마트폰 세로 화면에서도 가로 스크롤 없이 사용할 수 있어야 한다.
- 색상만으로 상태를 구분하지 않고 텍스트, 아이콘 또는 형태를 함께 사용한다.
- 키보드 탐색, 대체 텍스트, 명확한 포커스 표시 등 웹 접근성을 고려한다.

### 6.2 API 공통 규칙

- API 기본 경로는 `/api`를 사용한다.
- 요청·응답 본문은 특별한 사유가 없는 한 JSON을 사용한다.
- 오류 응답은 오류 코드, 사용자에게 표시 가능한 메시지 및 추적 식별자를 포함한다.
- 목록 API는 페이지 정보, 전체 또는 다음 페이지 존재 여부를 제공한다.
- 인증이 필요한 API는 사용자와 역할을 검증하고 객체 단위 접근 권한도 확인한다.
- 카드 이미지와 TTS 오디오 파일은 MIME 형식과 크기를 검증한다.

### 6.3 AI 서비스 인터페이스

- 추천 입력: 사용자 식별자, 현재 선택 카드, 시간 정보 및 사용할 수 있는 카드 집합
- 추천 출력: 순위가 있는 카드 목록, 알고리즘 버전 및 선택적 추천 근거 코드
- 문장 생성 입력: 순서가 보존된 카드 ID와 의미, 언어 및 사용자 표현 설정
- 문장 생성 출력: 생성 문장, 모델/규칙 버전, 안전성 처리 결과 및 처리 상태
- AI 서비스에 불필요한 개인 식별 정보를 전달하지 않는다.

## 7. 데이터 요구사항

| 데이터 개체 | 필수 주요 속성 | 관계 및 비고 |
|---|---|---|
| User | 사용자 ID, 역할, 표시명, 상태, 생성일 | 인증 정보는 안전한 방식으로 별도 관리 |
| UserLink | 보호자/교사 ID, PECS 사용자 ID, 관계, 상태 | 데이터 접근 범위 결정 |
| Category | 카테고리 ID, 이름, 표시 순서, 활성 상태 | 여러 카드 포함 |
| Card | 카드 ID, 카테고리 ID, 이름, 의미, 이미지 위치, 활성 상태 | 공통 또는 사용자 전용 구분 가능 |
| CommunicationSession | 세션 ID, 사용자 ID, 시작·완료 시각, 상태 | 선택·문장·TTS 기록 묶음 |
| CardSelection | 세션 ID, 카드 ID, 선택 순서, 선택 시각, 추천 경유 여부 | 중복 카드 허용 |
| GeneratedSentence | 세션 ID, 문장, 생성 버전, 생성 시각, 상태 | 재생성 이력 구분 |
| TtsEvent | 세션 ID, 문장 기록 ID, 요청 시각, 결과 | 음성 원본의 장기 저장 여부는 정책으로 결정 |
| RecommendationEvent | 사용자 ID, 입력 맥락, 추천 카드, 순위, 버전, 노출·선택 시각 | 추천 품질 평가에 활용 |
| AuditLog | 행위자, 작업, 대상, 결과, 발생 시각 | 관리자도 임의 수정 불가 |

모든 주요 데이터는 생성 시각을 저장하며 변경 가능한 개체는 수정 시각도 저장한다. 데이터베이스의 시각은 일관된 표준 시간대로 저장하고 화면에서 사용자 지역 시간대로 표시한다.

## 8. 비기능 요구사항

| ID | 분류 | 요구사항 및 검증 기준 |
|---|---|---|
| NFR-PERF-001 | 성능 | 정상 운영 조건에서 카드 목록·검색 API의 95%가 2초 이내 응답해야 한다. |
| NFR-PERF-002 | 성능 | 추천 결과는 95%가 3초 이내, 문장 생성 및 TTS 최초 응답은 95%가 각각 5초 이내여야 한다. 제한 초과 시 진행 또는 실패 상태를 알린다. |
| NFR-AVAIL-001 | 가용성 | 추천·문장 생성·TTS 중 한 기능의 장애가 카드 탐색과 선택을 중단시키지 않아야 한다. |
| NFR-SEC-001 | 보안 | 전송 구간은 운영 환경에서 HTTPS를 사용하고 비밀번호는 검증된 단방향 해시로 저장해야 한다. |
| NFR-SEC-002 | 보안 | 서버는 모든 입력의 형식, 길이 및 권한을 검증하고 업로드 파일의 형식과 크기를 제한해야 한다. |
| NFR-PRIV-001 | 개인정보 | 개인정보 수집 목적, 항목, 보존 기간 및 이용 동의를 고지하고 필요한 최소 정보만 수집해야 한다. 아동 등 대상 사용자 특성에 따른 법적 동의 요건은 출시 지역 기준으로 별도 검토한다. |
| NFR-ACC-001 | 접근성 | 핵심 기능은 키보드만으로 조작 가능하고 이미지에 대체 텍스트가 제공되어야 한다. 목표 기준은 WCAG 2.2 AA로 한다. |
| NFR-RESP-001 | 호환성 | 최신 주요 브라우저의 PC·태블릿·스마트폰 화면에서 핵심 이용 흐름이 동작해야 한다. 지원 버전 범위는 출시 전에 확정한다. |
| NFR-REL-001 | 신뢰성 | 카드 선택이나 문장 생성 요청의 재시도로 동일 이력이 의도치 않게 중복 저장되지 않도록 해야 한다. |
| NFR-OBS-001 | 관측성 | 서버 오류에는 추적 식별자를 부여하고 개인정보를 제외한 요청 결과, 지연 시간 및 오류를 로그로 남겨야 한다. |
| NFR-BACKUP-001 | 복구 | 운영 데이터의 백업 주기, 보존 기간, 복구 목표(RPO/RTO)를 운영 전 확정하고 정기적으로 복구를 시험해야 한다. |

## 9. 공통 예외 처리

| 상황 | 기대 동작 |
|---|---|
| 네트워크 연결 끊김 | 작성 중인 카드 선택을 가능한 범위에서 보존하고 연결 실패와 재시도 방법을 안내한다. |
| 인증 만료 | 입력 상태를 가능한 범위에서 보존한 뒤 재로그인을 요구한다. |
| 권한 없음 | 민감한 대상의 존재 여부를 노출하지 않으면서 접근 거부를 안내한다. |
| AI 서비스 지연·실패 | 선택 카드를 유지하고 재시도 또는 규칙 기반 대체 결과를 제공한다. |
| 잘못된 입력 | 문제가 있는 필드 가까이에 원인과 수정 방법을 표시한다. |
| 데이터 없음 | 오류가 아닌 빈 상태 화면과 다음 행동을 안내한다. |
| 중복 요청 | 멱등 키 또는 서버 측 검증으로 이력·문장이 중복 생성되지 않게 한다. |

## 10. MVP 범위 및 단계별 출시

### 10.1 1단계 MVP

- 카드·카테고리 조회, 검색 및 이미지 오류 대체 표시
- 문장 보드의 선택, 제거, 순서 변경 및 초기화
- 사용자별 선택 이력 저장
- 빈도·최근성·연관 규칙 기반 추천과 콜드 스타트 기본 추천
- 선택 카드 기반 한국어 문장 생성 및 실패 시 대체 처리
- TTS 재생·중지와 실패 처리
- 기본 역할 및 연결 사용자 접근 통제
- 핵심 분석 지표와 기간별 대시보드
- 관리자 헬스 체크 및 감사 로그

### 10.2 2단계 개선

- 개인 카드 등록과 고급 카드 관리
- 문장 편집 정책 및 사용자별 표현 설정
- 추천 알고리즘 고도화와 설명 정보
- 분석 데이터 파일 내보내기
- 음성 종류·속도 설정
- 오프라인 또는 설치형 웹 앱 지원 검토

## 11. 검수 시나리오

| ID | 시나리오 | 합격 조건 |
|---|---|---|
| AT-001 | 사용자가 `나 → 물 → 마시다` 카드를 순서대로 선택하고 문장을 생성한다. | 세 카드가 순서대로 보이고, 핵심 의미를 보존한 자연스러운 문장이 생성된다. |
| AT-002 | 생성된 문장의 음성 출력을 두 번 연속 요청한다. | 문장과 동일한 음성이 재생되며 두 음성이 겹치지 않는다. |
| AT-003 | 사용 이력이 없는 신규 사용자가 카드 화면에 진입한다. | 오류 없이 기본 추천 카드가 표시된다. |
| AT-004 | 추천 서비스를 중단한 상태에서 카드를 선택한다. | 추천 실패는 안내되지만 검색, 카드 선택 및 문장 보드는 계속 동작한다. |
| AT-005 | 보호자 A가 연결되지 않은 사용자 B의 대시보드 URL 또는 API에 접근한다. | 요청이 거부되고 사용자 B의 데이터가 노출되지 않는다. |
| AT-006 | 선택 카드의 순서를 변경한 후 문장을 생성한다. | 변경된 화면 순서와 문장 생성 입력 순서가 일치한다. |
| AT-007 | 이미지가 없는 카드를 조회하고 선택한다. | 대체 이미지와 카드명이 표시되며 카드를 선택할 수 있다. |
| AT-008 | 대시보드에서 특정 사용자와 기간을 선택한다. | 모든 수치와 그래프가 동일한 사용자·기간 조건으로 계산된다. |
| AT-009 | 스마트폰 세로 화면에서 카드 선택부터 음성 출력까지 수행한다. | 가로 스크롤이나 가려진 필수 버튼 없이 전체 흐름을 완료한다. |
| AT-010 | 동일한 완료 요청을 네트워크 재시도로 두 번 전송한다. | 의사소통 세션과 관련 이용 이력이 한 번만 저장된다. |

## 12. 요구사항 추적표

| 과제 목표 | 관련 요구사항 |
|---|---|
| 웹 기반 PECS 환경 | FR-CARD-001~006, FR-BOARD-001~007, NFR-RESP-001 |
| 개인 맞춤형 카드 추천 | FR-REC-001~006, FR-HIST-001~004 |
| 자연어 문장 생성 | FR-SENT-001~007 |
| TTS 음성 출력 | FR-TTS-001~005 |
| 이용 데이터 축적 | FR-HIST-001~006, FR-ADM-004 |
| 보호자·교사 대시보드 | FR-DASH-001~006 |
| 서비스 운영과 안전 | FR-AUTH-001~005, FR-ADM-001~004, NFR-SEC-001~002, NFR-PRIV-001 |

## 13. 미확정 사항

다음 항목은 이해관계자 협의 후 값 또는 정책을 확정해야 한다.

- 로그인 방식과 미성년 사용자 본인 계정 제공 여부
- 보호자/교사와 PECS 사용자의 연결 승인 절차
- 사용자 전용 카드 등록 주체 및 검수 절차
- 카드 이미지의 허용 파일 형식, 최대 크기와 저장 위치
- 문장 최대 길이, 카드 최대 선택 개수 및 문장 직접 편집 허용 범위
- 사용할 생성형 AI와 TTS 공급자, 비용 한도 및 장애 시 대체 방식
- 이용 이력, AI 요청 로그, 음성 파일 및 감사 로그의 보존 기간
- 개인정보 처리의 출시 국가별 법률·동의 요건
- 지원 브라우저의 구체적인 최소 버전
- 목표 동시 사용자 수, 가용성, 백업 RPO 및 RTO

위 미확정 사항이 결정되면 관련 기능 요구사항, 비기능 수치 및 테스트 케이스를 갱신하고 문서 버전을 올린다.
